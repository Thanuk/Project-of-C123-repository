function preload(){

}

function setup(){
    canvas = createCanvas(400, 400);
    canvas.position(950, 150);
    video = createCapture(VIDEO);
    video.size(550, 600);
}

function draw(){
    
}